import React from 'react';
import {createComponent} from '@lit/react';
import Avatar from '.';

export default createComponent({
  tagName: 'stylospectrum-avatar',
  elementClass: Avatar,
  react: React,
});
