import React from 'react';
import {createComponent} from '@lit/react';
import BusyIndicator from '.';

export default createComponent({
  tagName: 'stylospectrum-busy-indicator',
  elementClass: BusyIndicator,
  react: React,
});
