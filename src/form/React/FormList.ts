import React from 'react';
import {createComponent} from '@lit/react';
import FormList from '../form-list';

export default createComponent({
  tagName: 'stylospectrum-form-list',
  elementClass: FormList,
  react: React,
});
