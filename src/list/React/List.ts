import React from 'react';
import {createComponent} from '@lit/react';
import List from '..';

export default createComponent({
  tagName: 'stylospectrum-list',
  elementClass: List,
  react: React,
});
