import React from 'react';
import {createComponent} from '@lit/react';
import Menu from '..';

export default createComponent({
  tagName: 'stylospectrum-menu',
  elementClass: Menu,
  react: React,
});
