import React from 'react';
import {createComponent} from '@lit/react';
import MessageStrip from '.';

export default createComponent({
  tagName: 'stylospectrum-message-strip',
  elementClass: MessageStrip,
  react: React,
});
