import React from 'react';
import {createComponent} from '@lit/react';
import ProgressIndicator from '.';

export default createComponent({
  tagName: 'stylospectrum-progress-indicator',
  elementClass: ProgressIndicator,
  react: React,
});
