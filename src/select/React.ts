import React from 'react';
import {createComponent} from '@lit/react';
import Select from '.';

export default createComponent({
  tagName: 'stylospectrum-select',
  elementClass: Select,
  react: React,
});
