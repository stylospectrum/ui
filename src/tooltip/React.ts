import React from 'react';
import {createComponent} from '@lit/react';
import Tooltip from '.';

export default createComponent({
  tagName: 'stylospectrum-tooltip',
  elementClass: Tooltip,
  react: React,
});
